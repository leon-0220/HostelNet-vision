import session from "express-session";
import cors from "cors";

app.use(cors({
  origin: "https://leon-0220.github.io/HostelNet-vision", 
  credentials: true
}));

app.use(session({
  secret: "your_secret_key",
  resave: false,
  saveUninitialized: true,
  cookie: { 
    secure: true,
    maxAge: 1000 * 60 * 60 * 24 
  }
}));
